/* Create a simple component (under screens folder) displaying a text if user tries to 
navigate (e.g. by typing wrong address) into a page which does not exists.  */

export default function NotFound() { 
  return ( 
    <div>Not found</div> 
  )}